import { Context } from "grammy";

export async function handleProfile(ctx: Context) {
  // ⬇️ Hier kommt die bisherige Logik aus bot.command("profile", ...)
  // Beispiel:
  // await ctx.reply("Profil Wizard starten …");
}